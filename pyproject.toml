[tool.ruff]
target-version = "py310"
line-length = 100
extend-exclude = ["neutron_diffusion_tutorial.ipynb", "pytest-cache-files-*"]

[tool.ruff.lint]
select = ["F401", "F841"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
